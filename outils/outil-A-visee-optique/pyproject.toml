[project]
name = "visee-optique"
version = "0.1.0"
description = "Outil A — moteur de géométrie et de réfraction du protocole « Portion visible d'une cible éloignée au-dessus de la mer » v1.0"
requires-python = ">=3.9"

[tool.pytest.ini_options]
testpaths = ["tests"]
